<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="ga.hzcgw.springboot.dao.UserMapper">
    <!--<select id="queryUserByUserName" parameterType="String" resultType="ga.hzcgw.springboot.po.User">
        select id,
               user_name,
               user_pwd
        from tb_user
        where user_name = #{userName, jdbcType=VARCHAR}
    </select>
    <select id="queryUserById" parameterType="Integer" resultType="ga.hzcgw.springboot.po.User">
        select id,
               user_name,
               user_pwd
        from tb_user
        where id = #{id, jdbcType=INTEGER}
    </select>
    <select id="queryUserAll" resultType="ga.hzcgw.springboot.po.User">
        select *
        from tb_user
    </select>-->
</mapper>